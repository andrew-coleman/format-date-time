{
  "name": "format-date-time",
  "version": "1.0.0",
  "description": "Implementation of XPath's format-integer and format-dateTime functions",
  "main": "index.js",
  "scripts": {
    "atest": "node ./node_modules/mocha/bin/mocha test/**/*.js --reporter spec ",
    "test": "node ./node_modules/istanbul/lib/cli.js cover --report cobertura --report html ./node_modules/mocha/bin/_mocha -- --reporter mocha-jenkins-reporter \"test/**/*.js\"",
    "posttest": "istanbul check-coverage -statement 100 -branch 100 -function 100 -line 100"
  },
  "author": "Andrew Coleman",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.4.1",
    "istanbul": "^0.4.5",
    "jsdoc": "^3.4.0",
    "license-checker": "^6.0.0",
    "karma": "^1.2.0",
    "karma-chrome-launcher": "^2.0.0",
    "karma-coverage": "^1.1.1",
    "karma-mocha": "^1.1.1",
    "mocha": "^3.0.2",
    "mocha-jenkins-reporter": "^0.2.4"
  }
}
